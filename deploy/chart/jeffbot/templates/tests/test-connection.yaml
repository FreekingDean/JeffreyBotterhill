apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jeffbot.fullname" . }}-test-connection"
  labels:
    {{- include "jeffbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jeffbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
