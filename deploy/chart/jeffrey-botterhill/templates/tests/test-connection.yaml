apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jeffrey-botterhill.fullname" . }}-test-connection"
  labels:
    {{- include "jeffrey-botterhill.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jeffrey-botterhill.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
