apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeffrey-botterhill.fullname" . }}
  labels:
    {{- include "jeffrey-botterhill.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jeffrey-botterhill.selectorLabels" . | nindent 4 }}
